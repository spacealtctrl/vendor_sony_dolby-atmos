# Allow privileged apps to be clients of the DMS HAL
hal_client_domain(priv_app, hal_dms)

# Allow privileged apps to find the DMS hwservice
allow priv_app hal_dms_hwservice:hwservice_manager find;

# Binder communication with DMS HAL
binder_call(priv_app, hal_dms_default)
